services:
  - type: web
    name: acencia
    env: python
    plan: free
    buildCommand: |
      pip install -r backend/requirements.txt
      cd frontend
      npm install
      npm run build
    startCommand: "gunicorn -k uvicorn.workers.UvicornWorker backend.server:app"
